import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#040403",
    },
    secondary: {
      main: "#5B7553",
    },
    success: {
      main: "#8EB897",
    },
  },
});

export default theme;
